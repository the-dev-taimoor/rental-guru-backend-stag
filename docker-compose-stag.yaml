version: "3.8"

services:
  stag-api:
    build:
      context: .
    ports:
      - 8002:8000
    networks:
      - rental-guru-backend_rental-guru-backend-network

networks:
  rental-guru-backend_rental-guru-backend-network:
    external: true
